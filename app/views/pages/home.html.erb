<div class="jumbotron">
	<h1>Welcome to Ruben's app!</h1>
	
    <% if user_signed_in? %>
    <p> You are now signed in! </p>
    <% else %>
	<p>
		<%= link_to "Login", new_user_session_path, class: "btn btn-danger btn-lg" %>
		<%= link_to "Sign up", new_user_registration_path, class: "btn btn-danger btn-lg" %>
	</p>
	<% end %>
</div>